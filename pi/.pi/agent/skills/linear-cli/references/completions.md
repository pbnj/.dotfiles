# completions

Generate shell completion scripts.

```bash
source <(linear completions bash)   # bash
source <(linear completions zsh)    # zsh
linear completions fish | source    # fish
```

Add the appropriate line to `~/.bashrc`, `~/.zshrc`, or `~/.config/fish/config.fish`
to enable completions permanently.
