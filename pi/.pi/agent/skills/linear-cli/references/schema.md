# schema

Print the Linear GraphQL schema.

```bash
linear schema                           # SDL to stdout
linear schema --json                    # JSON introspection result
linear schema -o schema.graphql         # Write to file
```
