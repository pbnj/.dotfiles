# config

Interactively generate a `.linear.toml` configuration file for the current
directory. Sets project-level defaults such as team, project, and cycle.

```bash
linear config
```

Run this once per repository to avoid repeating `--team` and `--project` flags
on every command.
