# SENSIBLE

set-option -g automatic-rename on
set-option -g automatic-rename-format "#(~/.config/tmux/scripts/git-cwd.sh '#{pane_current_path}')"
set-option -g base-index 1
set-option -g default-command "${SHELL}"
set-option -g default-terminal 'tmux-256color'
set-option -g extended-keys-format csi-u # for pi
set-option -g focus-events on
set-option -g history-limit 20000
set-option -g mode-keys vi
set-option -g monitor-activity off
set-option -g mouse on
set-option -g pane-base-index 1
set-option -g renumber-windows on
set-option -g set-clipboard on
set-option -g status on
set-option -g status-interval 1
# Pending-chord indicator: names the active key table ("^B" for prefix, "ai"
# for the C-a agent table) and is blank in the root table. Fixed 4 columns so
# the window list never shifts when a chord is armed.
#
# The style sits OUTSIDE #{p4:} because #[...] sequences count as literal
# characters when padding. Note also that the argument of #{pN:} is resolved as
# a variable name unless it contains a nested #{...} - "#{p4:ab}" yields four
# spaces, not "ab  " - hence the nested conditional rather than a flat one.
set-option -g status-left '#[fg=colour162,bold]#{p4:#{?#{==:#{client_key_table},root},,#{?#{==:#{client_key_table},prefix},^B,#{client_key_table}}}}#[default]'
set-option -g status-left-length 4
set-option -g status-position top
set-option -g status-right ""
set-option -g status-right-length 0
set-option -g status-style bg=default
# -ga appends: plain -g would clobber tmux's built-in defaults (xterm* clipboard,
# ccolour, cstyle, focus, title). hyperlinks enables OSC 8 passthrough.
set-option -ga terminal-features ',*:RGB:hyperlinks'
set-option -g visual-activity off
set-option -g wrap-search off
set-option -ga terminal-overrides ',*:enacs@:smacs@:rmacs@:acsc@'
set-option -gq allow-passthrough on
set-option -gw window-status-current-style bg=default,bold
# Active pane: magenta (#d70087) reads clearly on both light and dark backgrounds
# (~5:1 contrast on white, ~4:1 on black). 256-colour cube index, so themes that
# remap the 0-15 base palette cannot wash it out. Inactive panes stay gray.
set-option -g pane-border-style 'fg=colour8'
set-option -g pane-active-border-style 'fg=colour162,bold'
# Per-pane title bar; active label matches the active border, inactive label gray
set-option -g pane-border-status top
# Coding agents publish their state as @agent_* pane options (claude from its
# status line, pi from the tmux-pane-status extension, both via
# ~/.local/bin/tmux-agent-status), so every agent pane shows its model, effort,
# repo/branch and usage at once instead of just "claude".
#
# The usage slot differs by agent because their limits do: claude reports 5h/7d
# rate-limit consumption, which is what constrains the next hour of work, while
# pi has no such limits and reports session token totals instead. Whichever
# arrived is what shows.
#
# Reset countdowns only advance when the agent republishes, so an idle claude
# pane can show a stale "resets in" - the same staleness its own status line has.
# tmux cannot recompute it: there is no current-time format variable, and
# pane-border-format is not passed through strftime.
#
# Reset countdowns render bare - "5h:8% (2h55m)" - because the 5h/7d label
# already says which clock the parenthetical belongs to. The status line spells
# out "(resets in 2h 55m)"; it has the room and the border does not.
#
# Three width tiers, kept as separate options because one line holding all of
# them is unreadable. "E:" expands an option's *content* as a format in the
# drawing pane's context, so #{@agent_*} inside still resolves per pane. E:
# expands twice - one level of indirection - so each tier must be self-contained
# rather than composing shared sub-options, hence the duplication between wide
# and medium. Every segment carries its own #{?} guard so a field the agent has
# not reported yet leaves no orphan " | " behind.
set-option -g @border_wide '#{@agent_name}#{?#{@agent_model}, #{@agent_model}#{?#{@agent_effort},/#{@agent_effort},},}#{?#{@agent_repo}, | #{@agent_repo}#{?#{@agent_branch},@#{@agent_branch},},}#{?#{@agent_ctx}, | ctx:#{@agent_ctx},}#{?#{||:#{@agent_5h},#{@agent_7d}},#{?#{@agent_5h}, | 5h:#{@agent_5h}#{?#{@agent_5h_reset}, (#{@agent_5h_reset}),},}#{?#{@agent_7d}, | 7d:#{@agent_7d}#{?#{@agent_7d_reset}, (#{@agent_7d_reset}),},},#{?#{@agent_tokens}, | #{@agent_tokens},}}'
set-option -g @border_medium '#{@agent_name}#{?#{@agent_model}, #{@agent_model}#{?#{@agent_effort},/#{@agent_effort},},}#{?#{@agent_branch}, | #{@agent_branch},}#{?#{@agent_ctx}, | ctx:#{@agent_ctx},}#{?#{||:#{@agent_5h},#{@agent_7d}},#{?#{@agent_5h}, | 5h:#{@agent_5h},}#{?#{@agent_7d}, | 7d:#{@agent_7d},},#{?#{@agent_tokens}, | #{@agent_tokens},}}'
set-option -g @border_narrow '#{@agent_name}#{?#{@agent_ctx}, ctx:#{@agent_ctx},}'
# 90 columns is the wide cutoff because the longest claude row renders in 83, and
# a vertical split of a full-width terminal leaves about 100 - so a side-by-side
# pair keeps its reset countdowns instead of degrading to bare percentages.
# The width tests use "e|>=|" (arithmetic): the plain "#{>=:}" comparison is a
# string compare, under which a 50-column pane tests wider than 90. The
# pane_current_command test is a liveness guard - a pane whose agent has exited
# falls back to the command name instead of showing stale metadata.
set-option -g pane-border-format ' #{?pane_active,#[fg=colour162#,bold],#[fg=colour8]}#{pane_index}: #{?#{&&:#{!=:#{@agent_name},},#{m/r:^(claude|pi|node)$,#{pane_current_command}}},#{?#{e|>=|:#{pane_width},90},#{E:@border_wide},#{?#{e|>=|:#{pane_width},60},#{E:@border_medium},#{E:@border_narrow}}},#{pane_current_command}} #[default]'
set-option -s escape-time 0
set-option -s extended-keys on

if-shell '[[ $(uname -s) == Darwin ]]' { set -s copy-command 'pbcopy' }

###############################################################################
# KEYBINDINGS
###############################################################################

## Create 'C-a' chain for ai keybinds
# refresh-client -S forces the status redraw: tmux flags one when the prefix
# flag toggles, but not reliably when switch-client -T enters a custom table.
bind-key C-a { switch-client -T ai ; refresh-client -S }
bind-key -T ai p split-window -c '#{pane_current_path}' -- pi
bind-key -T ai c split-window -c '#{pane_current_path}' -- claude --dangerously-skip-permissions
bind-key -T ai g split-window -c '#{pane_current_path}' -- copilot --yolo

## Quick scroll/copy mode entry
bind-key Space copy-mode

## productivity
bind-key C-] run-shell ${HOME}/.local/bin/tmux-scrollback
bind-key C-f new-window -- nvim -c 'Projects!'
bind-key f run-shell ${HOME}/.local/bin/fzf-projects
bind-key C-/ new-window -c ${HOME}/Projects -- nvim -c 'lua Snacks.picker.grep({ title = "Grep (all projects)", dirs = { "~/Projects/" }, layout = { fullscreen = true } })'
bind-key C-s split-window -- ${HOME}/.local/bin/search

## Smooth scrolling with mouse wheel (already enabled with 'mouse on')
## Use Shift+PageUp/PageDown for history outside copy mode
bind-key -n S-PageUp copy-mode -u
bind-key -n S-PageDown copy-mode -u

## Copy/scroll mode navigation
bind-key -T copy-mode Escape send-keys -X cancel
bind-key -T copy-mode-vi Escape send-keys -X cancel
bind-key -T copy-mode-vi i send-keys -X cancel
bind-key -T copy-mode-vi v send -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection

## Open URL under cursor (select word, pipe to open)
set -g word-separators " \t\n"
bind-key -T copy-mode-vi o send-keys -X select-word \; send-keys -X copy-pipe-and-cancel "$HOME/.config/tmux/scripts/open-url.sh"

## Vi keys for history scrolling in copy mode
bind-key -T copy-mode-vi j send -X cursor-down
bind-key -T copy-mode-vi k send -X cursor-up
bind-key -T copy-mode-vi h send -X cursor-left
bind-key -T copy-mode-vi l send -X cursor-right
bind-key -T copy-mode-vi '/' command-prompt -p 'search forward:' 'send -X search-forward "%%%"'
bind-key -T copy-mode-vi '?' command-prompt -p 'search backward:' 'send -X search-backward "%%%"'
bind-key -T copy-mode-vi n send -X search-again
bind-key -T copy-mode-vi N send -X search-reverse
bind-key -T copy-mode-vi g send -X history-top
bind-key -T copy-mode-vi G send -X history-bottom
bind-key -T copy-mode-vi C-d send -X halfpage-down
bind-key -T copy-mode-vi C-u send -X halfpage-up
bind-key -T copy-mode-vi C-f send -X page-down
bind-key -T copy-mode-vi C-b send -X page-up

## Prompt navigation in copy mode
bind-key -T copy-mode-vi '[' send -X previous-prompt
bind-key -T copy-mode-vi ']' send -X next-prompt

## Command history navigation (edit mode)
bind-key -T edit-mode-vi Down send-keys -X history-down
bind-key -T edit-mode-vi Up send-keys -X history-up

## Quick access to prompt/history
bind-key Enter {
  copy-mode ;
  send-keys -X previous-prompt ;
  send-keys -X select-line ;
  send-keys -X next-prompt ;
}

## from: https://github.com/tmux-plugins/tmux-pain-control/blob/master/pain_control.tmux
### Window nav
bind-key '<' swap-window -t -1\; select-window -t -1
bind-key '>' swap-window -t +1\; select-window -t +1
bind-key C-n select-window -n
bind-key C-p select-window -p
bind-key -n M-] next-window
bind-key -n M-[ previous-window

## Spliting windows
bind-key - split-window -v -c '#{pane_current_path}'
bind-key '\' split-window -h -c '#{pane_current_path}'
bind-key '%' split-window -h -c "#{pane_current_path}"
bind-key '"' split-window -v -c "#{pane_current_path}"

## New windows inherit the current pane's cwd (default is the session start dir)
bind-key c new-window -c "#{pane_current_path}"

## Panes nav
bind-key  C-h  select-pane  -L
bind-key  C-j  select-pane  -D
bind-key  C-k  select-pane  -U
bind-key  C-l  select-pane  -R
bind-key  H    join-pane    -h  -t  !
bind-key  J    join-pane    -v  -t  !

unbind C-o
bind-key C-o select-pane -t+

set-option -g window-status-format '#I:#W#{?window_flags,#{window_flags}, }'
set-option -g window-status-current-format '#I:#W#{?window_flags,#{window_flags}, }'
